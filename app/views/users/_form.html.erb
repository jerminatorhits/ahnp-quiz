<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :agent %>
    <%= form.text_field :agent, id: :user_agent %>
  </div>

  <div class="field">
    <%= form.label :counter %>
    <%= form.number_field :counter, id: :user_counter %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
